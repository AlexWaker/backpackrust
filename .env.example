# ------------ 基础连接配置 (一般无需修改) ------------
# REST API 基础 URL
BP_API_URL=https://api.backpack.exchange
# WebSocket 基础 URL (行情 / 订单推送)
BP_WS_URL=wss://ws.backpack.exchange
# API 公钥 (从交易所获取) — 用于鉴权头部 X-API-Key
BP_API_KEY=
# API 私钥 / 签名密钥 (从交易所获取) — 用于生成请求签名
BP_SECRET_KEY=
# 请求时间窗口 (毫秒)。客户端时间戳与服务器允许的最大偏差。过大可能拒绝；过小易受本地时钟漂移影响。
BP_WINDOW=5000

# ------------ 交易参数 (按策略可调整) ------------
# 交易对符号。例如：APT_USDC_PERP / SOL_USDC_PERP / BTC_USDC_PERP
BP_SYMBOL=SOL_USDC_PERP
# 下单数量。单位：标的数量 (例如 SOL 的个数)。市价或限价单都会使用此基础数量，策略可在内部覆盖。
BP_QTY=0.01
# 价格最小跳动 tick。用于四舍五入 / 计算偏移价差；单位：报价币种价格。
BP_TICK=0.01

# ------------ 风控参数 (降低风险/爆仓概率) ------------
# 最大允许净仓位 (标的数量)。|净仓位| >= MAX_NET 时禁止继续加仓；只允许减仓或等待。
MAX_NET=2000
# 重报价 / 重新挂单的最小间隔 (毫秒)。降低过快撤挂导致的频率限制风险。
REPRICE_MS=500
# 遇到严重错误 / 频繁拒单 / 断线后暂停交易的时长 (毫秒)。
PAUSE_MS=30000

# ------------ 网格做市 (Drip) 相关参数 (若未使用可忽略) ------------
# 是否启用 Drip 网格逻辑 (true/false)。禁用则忽略以下所有 Drip 参数。
DRIP_ENABLE=true
# Drip 下单间隔 (毫秒)。每隔该时间尝试放置一笔小额挂单。
DRIP_MS=1800
# 每笔 Drip 挂单的数量 (标的数量)。
DRIP_QTY=0.01
# 挂单价相对参考价 (如最优价或中间价) 的偏移 tick 数。正值通常用于在盘口外安全放单。
DRIP_OFFSET_TICKS=1
# 允许的最大在途挂单总数。避免挂单过多占用保证金与触发频率限制。
MAX_TOTAL_ORDERS=180
